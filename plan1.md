Необходимо реализовать дипломную работу по теме:
Магистерская диссертация: Применение обратного обучения с подкреплением для моделирования поведения агентов в многоагентной среде

Шаги:
1. Реализуем прототип многоагентной среды. Необходимо создать на python скрипт, реализующий 3 дорожные полосы (1 основную трассу - main_line и 2 дополнительных трасы - highway1 и highway2) в виде очередей ограниченной длины. В конец очереди main_line раз в main_delay милисекунд добавляется машина (число 1). Далее в соответствии с флагом line_choose, принимающим значение 1 или 2, мы решаем, в конец какой из двух других очередей (highway1 или highway2) поместить значение машины (число 1). Если очередь, в которую мы готовы направить машину, не забита до предела (число элементов в очереди не достигло максимума), то элемент машины (со значением 1) удаляется из начала основной очереди (main_line) и помещается в конец соответсвующей выбранной очереди, в противном же случае, этого не происходит, пока выбранная очередь все еще полная.  Из каждой очереди убирается первый элемент раз в highway_delay милисекунд, для каждой из двух очередей (highway1 или highway2) значение highway_delay разное и задается в коде. Также в коде задается максимальное число элементов capacity каждой из двух очередей (highway1 или highway2) и максимальное число элементов main_line_capacity очереди main_line.   

Необходимо сделать визуализацию через web интерфейс, которая в режмие реального времени будет отображать состояния очередей. В правой части окна находится одна горизонтальная серая полоса, отображающая состояние очереди main_line, левее нее находятся на том же уровне 2 параллельные серые полосы, отображающие состояние очередей highway1 и highway2. Начало каждой очереди - справа, конец очереди - слева, каждая машина (значение 1 внутри массива) отображается как черный сегмент на серой линии полосы с правого края. Если очередь полная, то вся линия черная, если очередь пустая, то вся линия серая, в противном случае, если, например, в очереди с capacity = 20 будет находиться 5 машин, то левые 75% дороги будут серые, а остальные 25% справа будут черными. На визуализации также присутствует кнопка, которая в режиме реального времени меняет параметр line_choose, принимающий значение 1 или 2, который определяет, в конец какой из двух других очередей (highway1 или highway2) поместить значение машины (число 1).

Правки:
1.1 Поменяй местами main_line и highway так, чтобы слева была main_line, а справа 1 и 2 highway. Сделай так, чтобы у каждого из highway была своя скорость, которую можно было бы контролировать отдельно, а не одна скорость для всех.

1.2 Сделай так, чтобы напротив значения переменной отображалось не начальное, а текущее значение ползунка под ней. Остальное не меняй.

1.3 Увеличь шаг для ползунков скорости highway1 и highway2 с 500 мс до 100 мс.

1.4 Переверни все очереди на визуализации так, чтобы начало их было справа, а конец слева, то есть отзеркаль их.

Сделай git commit и git push в новый репозиторий MAS IRL Diplom 2026 

2. Создай новый файл в папке prototype, который будет называться rnd_simulation.py Он содержит все то же самое, что и файл simulation.py, но к общей логике добавляется еще одна кнопка: random, которая меняет значение скорости всех полос (main_line, highway1 и highway2) на случайное в пределах возможных значений каждого из ползунков и отображает его напротив значения переменной, также ставит ползунок на выбранное (рандомное) значение.

3. Создай новый файл в папке prototype, который будет называться mas_simulation.py Добавь функцию can_take2, которая будет опираться на число машин в higwhway2, эта функция возвращает 1, если число меньше threshhold (например, 5), иначе возвращает 0. Также в коде появляется новый параметр can_take1, который будет регулироваться пользователем через интерфейс (кнопка) и сможет принимать значение 0 или 1. Возможность выбора highway1 или highway2 напрямую через кнопки теперь отсутствует, выбор highway1 или highway2 происходит следующим образом: 
1. если can_take1 = 0 и can_take2 = 0, то объект не добавляется ни в одну из очередей, пока состояние не изменится, 
2. если can_take1 = 1 и can_take2 = 1, то выбирается случайная из 2 очередей с шансом 50% (highway1 или highway2), 
3. если can_take1 = 1 и can_take2 = 0, то объект отправляется в highway1,
4. если can_take1 = 0 и can_take2 = 1, то объект отправляется в highway2.
Если can_take2 = 0 на визуализации, то очередь highway2 подсвечивается красным, иначе без изменения. 

3.1 Добавь в файл mas_simulation.py весь недостающий функционал из файла: 
К общей логике добавляется еще одна кнопка: random, которая меняет значение скорости всех полос (main_line, highway1 и highway2) на случайное в пределах возможных значений каждого из ползунков и отображает его напротив значения переменной, также ставит ползунок на выбранное (рандомное) значение.

5. Значением can_take2 можно также менять с визуализации (через ползунок длины от 1 до максимальной длины ряда). При нажатии кнопки random оно также меняется. Изменения вноси в файл mas_simulation.py.

6. Основываясь на файле mas_simulation.py, создай файл log_mas_simulation.py, в котором будут следующие изменения. Необходимо добавить каждые 100 миллисекунд логирование в dataframe таких данных, как число элементов в массиве highway1 в момент фиксации, значение drain_delay_h1 в момент фиксации, значение can_take2 в момент фиксации, timestamp момента фиксации до миллисекунд. Логирование начинается по нажатию кнопки start_logging, которую нужно отобразить как в коде, так и на визуализации. Также нужна аналогичная кнопка stop_logging, при нажатии на которую логирование завершается, и в папке ./expert создается файл .csv с названием, создаваемым так: префикс expert_ + текущее время (до секунд).

1